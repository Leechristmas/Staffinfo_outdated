<Window x:Class="Staffinfo.Desktop.View.AddMilitaryUnitView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Staffinfo.Desktop.View"
        mc:Ignorable="d"
        Title="Добавить воинскую часть" Height="140" Width="400">
    <Grid>
        <StackPanel Orientation="Vertical"
                    Margin="5">
            <StackPanel Orientation="Horizontal"
                        Margin="5">
                <TextBlock Text="Название/номер части:"
                           FontSize="14"
                           VerticalAlignment="Center"/>
                <TextBox Name="MilitaryUnitTBox"
                         Margin="10,0"
                         MinWidth="200"
                         FontSize="14"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        Margin="5">
                <TextBlock Text="Описание:"
                           FontSize="14"
                           VerticalAlignment="Center"/>
                <TextBox Name="DescriptionTBox"
                         Margin="10,0"
                         MinWidth="284"
                         FontSize="14"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        Margin="5"
                        HorizontalAlignment="Right">
                <Button Name="AddOrganizationUnit"
                        Content="Добавить"
                        MinWidth="100"
                        Style="{StaticResource ButtonStyle}"/>
                <Button Name="CloseBtn"
                        Content="Закрыть"
                        MinWidth="100"
                        Style="{StaticResource ButtonStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
